# Example test projects

This directory will contain reusable SkillConsole project and test definitions that target the public example Skills.

Examples must use placeholder Endpoint and Secret references. They must not contain real API keys, organization headers, or private fixtures.
