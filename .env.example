# Optional registry or mirror prefix. Include the trailing slash.
# Leave empty to use Docker Hub.
DOCKER_REGISTRY=

# Change this value before exposing a production deployment.
POSTGRES_PASSWORD=skillconsole
